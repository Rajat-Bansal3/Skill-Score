// const startTime = String(
//     game.pgn.match(/\[StartTime\s+"(\d{2}:\d{2}:\d{2})"\]/)
//   ).split(",")[1];
//   const date = (game.end_time - timeToSeconds(startTime!)) * 1000;

//   for time in ms end - start so we could add even more vesatality
